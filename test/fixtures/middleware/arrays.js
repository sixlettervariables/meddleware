'use strict';

module.exports = function (config) {
    var middleware = [];
    
    middleware.push(function ArrayFirst(req, res) {
    
    });
    
    middleware.push(function ArraySecond(req, res) {
    
    });
    
    return middleware;
};
